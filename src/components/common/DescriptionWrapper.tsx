import styled from 'styled-components';

export const DescriptionWrrapper = styled.div`
    padding: 1rem;
    background-color: ${({ theme }) => theme.backgroundLight};
    border: 1px solid ${({ theme }) => theme.border};
`;
